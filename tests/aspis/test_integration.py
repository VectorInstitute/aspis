"""Sample integration test so the CI pipeline passes."""

import pytest


# Remove this or replace with an actual integration test
@pytest.mark.integration_test
def test_foo() -> None:
    """Sample integration test so the CI pipeline passes."""
    assert True
